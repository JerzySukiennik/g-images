# secrets — never commit
.env

# environment
.venv/

# dataset binaries and checkpoints (gigabytes, regenerable from scripts)
data/*.bin
data/*.json
checkpoints/
run/

# python
__pycache__/
*.pyc
.DS_Store

# Kaggle orchestration working dir (checkpoints, kernel metadata)
kaggle-run/

# ...but the AnyEdit shard->type index IS source: 28 KB, built by scanning all
# 383 parquet footers, and data/fetch_anyedit.py cannot pick shards without it.
# It was silently excluded by the rule above once already, which failed all three
# prep kernels at startup.
!data/anyedit_shardmap.json
